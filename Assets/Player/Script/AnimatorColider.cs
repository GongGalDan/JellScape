using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimatorColider : MonoBehaviour
{/*
    Items items;
    Animator animator;
    BoxCollider colliderWeapon;
    GameObject objectWeapon;



    void Start()
    {
        items = GetComponent<Items>();
        animator = GetComponent<Animator>();

        colliderWeapon = objectWeapon.GetComponentInChildren<BoxCollider>();

        colliderWeapon.enabled = false;
    }

    private void Update()
    {
        FindOjbectWeapon();
    }

    void FindOjbectWeapon()
    {
       if (items.currentItems.Count == 0)
            return;

       if( items.currentItems[0] == gameObject.CompareTag("Apolo"))
        {
            objectWeapon = items.currentItems[0];
        }

        if (items.currentItems[0] == gameObject.CompareTag("Stick"))
        {
            objectWeapon = items.currentItems[0];
        }
    }


    public void AttackStart()
    {
        colliderWeapon.enabled = true;
    }

    public void AttackEnd()
    {
        colliderWeapon.enabled = false;
    }
    */
}
